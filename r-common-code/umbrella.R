# Include file for common code
if(!exists(HistPercent(), mode = "function")) source("charts.R")
if(!exists(FactorToNumeric(), mode = "function")) source("strings.R")
if(!exists(dayLevels, mode = "numeric")) source("times.R")
